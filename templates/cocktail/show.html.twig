{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
    <div class="container mx-auto px-6">
        <div class="md:flex md:items-center">
            <div class="w-full h-64 md:w-1/2 lg:h-96">
                <img class="h-full w-full rounded-md bg-cover bg-center max-w-lg mx-auto"
                     src="{{ cocktail.url }}"
                     alt="{{ cocktail.name }}">
            </div>
            <div class="w-full max-w-lg mx-auto mt-5 md:ml-8 md:mt-0 md:w-1/2">
                <h3 class="text-gray-700 uppercase text-lg">{{ cocktail.name }}</h3>
                <span class="text-gray-500 mt-3">{{ cocktail.price }}€</span>
                <hr class="my-3">
                <div class="mt-2">
                    <span class="text-gray-700 text-sm">Quantity: <span class="font-bold">{{ count }}</span></span>
                    <p class="mt-4">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Atque eaque eligendi error expedita,
                        ipsam provident sint! Aspernatur at dolorem dolores explicabo odit officia qui quis, repellendus
                        sint ullam velit voluptate!</p>
                </div>
                <form method="post" action="{{ path('cart_add', {cocktail: cocktail.id}) }}" class="mt-6">
                    <button class="px-8 py-2 bg-indigo-600 text-white text-sm font-medium rounded hover:bg-indigo-500 focus:outline-none focus:bg-indigo-500">
                        <svg class="h-5 w-5" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                             viewBox="0 0 24 24" stroke="currentColor">
                            <path d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
                        </svg>
                    </button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}


